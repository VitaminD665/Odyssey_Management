# Unit and Integration Tests


This directory houses the unit and integration tests for the repository. 

The default testing framework used is pytest. Used to validate the business logic anbd related API calls.

Is ignored by the docker image

